// Plain TypeScript file with no JSDoc comments

export function add(a: number, b: number): number {
  return a + b;
}

export const PI = 3.14159;

export class Calculator {
  calculate(x: number, y: number): number {
    return x * y;
  }
}

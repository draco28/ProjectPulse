/**
 * This function has a malformed JSDoc comment
 * Missing closing tag
 *
 * @param value - Some value
 * @returns The processed value

export function processValue(value: string): string {
  return value.toUpperCase();
}

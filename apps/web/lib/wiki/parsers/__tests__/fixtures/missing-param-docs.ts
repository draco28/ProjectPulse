/**
 * Multiplies two numbers together
 *
 * This function has parameters but no @param documentation
 *
 * @returns The product of the two numbers
 */
export function multiply(a: number, b: number): number {
  return a * b;
}

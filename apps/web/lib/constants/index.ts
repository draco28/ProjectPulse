/**
 * Constants - Barrel Export
 *
 * Central export for all application constants.
 * Currently includes the modular status system.
 *
 * @example
 * ```typescript
 * import { TICKET_STATUSES, TicketStatusSystem } from '@/lib/constants';
 * ```
 */

export * from './status';

# MCP Authentication Documentation - Research Report

**Date:** 2025-11-22
**Sprint:** 9 (Post-Implementation)
**Status:** Research Complete - Ready for Implementation
**Token Usage:** ~85K / 200K (42%)

---

## Executive Summary

The MCP bearer token authentication system was fully implemented in Sprint 9, including:
- Token generation UI in Project Settings
- Bearer token validation middleware in MCP server
- Bcrypt-based token storage in database
- HTTP clipboard fallback for token copying

**CRITICAL GAP IDENTIFIED:** The implementation is complete and production-ready, but **completely undocumented** for end users. There is no guide explaining how to generate tokens, configure agents, or manage token security.

---

## 1. Current Documentation Inventory

### Existing Documentation

**MCP Documentation:**
- `/Users/draco/projects/AI_HUB/docs/MCP_ARCHITECTURE.md` - Technical architecture (v2.0.0, Sprint 8.7)
- `/Users/draco/projects/AI_HUB/docs/MCP_QUICK_START_v2.md` - Setup guide (mentions tokens, lacks workflow)
- `/Users/draco/projects/AI_HUB/docs/features/mcp-multi-agent-setup.md` - Multi-agent setup (brief token mention)

**User Guides:**
- `/Users/draco/projects/AI_HUB/docs/guides/onboarding-user-guide.md` - Onboarding process
- `/Users/draco/projects/AI_HUB/docs/guides/onboarding-agent-guide.md` - Agent perspective

**Product Documentation:**
- `/Users/draco/projects/AI_HUB/docs/README.md` - Documentation index
- `/Users/draco/projects/AI_HUB/docs/01-PRD.md` through `13-Project-Plan.md` - Product specs

### Documentation Gaps (CRITICAL)

❌ **NO dedicated authentication setup guide**
- Token generation workflow not documented
- Settings page navigation not explained
- One-time token display not emphasized

❌ **NO security best practices guide**
- Token storage recommendations missing
- Expiration policy guidance missing
- Revocation workflow not documented

❌ **NO token management documentation**
- Active token management not explained
- Last used monitoring not documented
- Token rotation strategy missing

❌ **Incomplete MCP setup guides**
- `MCP_QUICK_START_v2.md` mentions tokens in Step 2 (line 41) but lacks workflow
- `mcp-multi-agent-setup.md` shows config pattern but not token acquisition
- No end-to-end setup walkthrough

---

## 2. Wiki Structure & Schema

### WikiPage Model Analysis

**Schema Location:** `/Users/draco/projects/AI_HUB/apps/web/prisma/schema.prisma` (lines 740-809)

**Key Fields:**
```prisma
model WikiPage {
  id        Int     @id @default(autoincrement())
  projectId Int     // Sprint 8.9: Project scoping
  title     String
  content   String  @db.Text // Markdown content
  excerpt   String? @db.Text // List view summary
  category  String? // Grouping (e.g., "Setup", "Getting Started")
  path      String  @unique // URL-friendly (/setup/mcp-authentication)
  tags      String[] @default([]) // Search keywords
  
  // Hierarchy
  parentId Int?
  parent   WikiPage? @relation("PageHierarchy")
  children WikiPage[] @relation("PageHierarchy")
  
  // Metadata
  views        Int @default(0)
  revisions    Int @default(1)
  contributors Json @default("[]")
  autoGenerated Boolean @default(false) // US-107: Generated from JSDoc
  sourceFiles   String[] @default([]) // Source paths for auto-generated pages
}
```

**Web UI Routes:**
- `/wiki/page.tsx` - Wiki list view
- `/wiki/new/page.tsx` - Create new page
- `/wiki/[slug]/page.tsx` - View page
- `/wiki/[slug]/edit/page.tsx` - Edit page
- `/wiki/analytics/page.tsx` - Analytics dashboard

### Existing Wiki Pages

**Current Status:** Unknown (requires database query)

**Expected Categories:**
- "Getting Started" or "Setup" category should exist
- MCP-related pages may exist but not authentication-specific

### Wiki Creation Options

**Option A: Manual via Web UI**
- Navigate to `/wiki/new`
- Fill in form (title, content, category, tags)
- Submit to create database record

**Option B: MCP Tool Invocation**
- Use `wiki.create` tool
- Requires projectId, title, content, category
- Example in `apps/mcp-server/src/tools/wiki/`

**Option C: Database Seed Script**
- Add to `apps/web/prisma/seed.ts`
- Creates page on database reset
- Not ideal for production docs

**Option D: Migration Script**
- Create Prisma migration with INSERT statement
- Ensures page exists in all environments
- Recommended for critical documentation

---

## 3. Authentication Implementation Review

### MCP Server Implementation

**File:** `/Users/draco/projects/AI_HUB/apps/mcp-server/src/index-http.ts`

**Bearer Auth Middleware (Lines 107-166):**
```typescript
// Middleware: Agent Bearer Auth (Sprint 9)
app.use('/mcp', async (req, res, next) => {
  const authHeader = req.headers.authorization;
  
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return res.status(401).json({
      jsonrpc: '2.0',
      error: { code: -32001, message: 'Unauthorized: Missing bearer token' },
      id: null,
    });
  }

  const rawToken = authHeader.slice('Bearer '.length);

  try {
    // Validate token via web app (MCP never hits DB directly)
    const agentAuth = await httpClient.post<{ projectId: number; tokenId: number; name: string }>(
      '/api/agent-auth/validate',
      { token: rawToken }
    );
    
    // Attach agent auth to request for tools
    (req as any).agentAuth = agentAuth; // { projectId, tokenId, name }
    
    next();
  } catch (error) {
    return res.status(401).json({
      jsonrpc: '2.0',
      error: { code: -32001, message: 'Unauthorized: Invalid or expired token' },
      id: null,
    });
  }
});
```

**Key Features:**
✅ Validates `Authorization: Bearer <token>` header
✅ Delegates validation to web app (`/api/agent-auth/validate`)
✅ Attaches `agentAuth` context to request (projectId, tokenId, name)
✅ Returns JSON-RPC error on failure (401 Unauthorized)

### Web App Validation Endpoint

**File:** `/Users/draco/projects/AI_HUB/apps/web/app/api/agent-auth/validate/route.ts`

**Token Validation (Lines 22-52):**
```typescript
export async function POST(request: Request) {
  try {
    const body = await request.json();
    const { token } = validateRequestSchema.parse(body);

    // Validate token (throws on invalid/expired/revoked)
    const result = await validateProjectToken(token);

    return NextResponse.json(result, { status: 200 });
  } catch (error: any) {
    return NextResponse.json(
      { error: 'Invalid or expired token' },
      { status: 401 }
    );
  }
}
```

**Validation Function:** `validateProjectToken(token)` (imported from `@/lib/agent-tokens`)
- Uses bcrypt to compare token with stored hash
- Checks expiration date
- Checks revocation status
- Returns `{ projectId, tokenId, name }` on success

### Database Schema

**File:** `/Users/draco/projects/AI_HUB/apps/web/prisma/schema.prisma` (lines 338-355)

**ProjectToken Model:**
```prisma
model ProjectToken {
  id        Int      @id @default(autoincrement())
  projectId Int
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)

  name      String   // Human-friendly label (e.g., "Frontend Claude", "Backend Claude")
  tokenHash String   // bcrypt hash of opaque token (plaintext never stored)
  expiresAt DateTime?
  lastUsedAt DateTime?
  isRevoked Boolean  @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([projectId, name]) // One token per name per project
  @@index([projectId, isRevoked, expiresAt]) // Fast validation queries
  @@map("project_tokens")
}
```

**Security Features:**
✅ Token plaintext NEVER stored (only bcrypt hash)
✅ Project-scoped (multi-tenancy)
✅ Expiration support
✅ Revocation support
✅ Last used tracking
✅ Unique constraint (one token per name per project)

### Settings UI Implementation

**File:** `/Users/draco/projects/AI_HUB/apps/web/app/projects/[id]/settings/ProjectSettingsClient.tsx`

**Token Generation UI (Lines 49-81):**
- Modal form with token name and expiry days
- POST to `/api/projects/${projectId}/tokens`
- Receives plaintext token ONCE (never shown again)
- Displays generated token in success modal

**HTTP Clipboard Fallback (Lines 125-157):**
```typescript
const copyToClipboard = (text: string) => {
  // Fallback for HTTP contexts where navigator.clipboard is unavailable
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(text)
      .then(() => alert('Copied to clipboard!'))
      .catch(() => fallbackCopyToClipboard(text));
  } else {
    fallbackCopyToClipboard(text);
  }
};

const fallbackCopyToClipboard = (text: string) => {
  const textArea = document.createElement('textarea');
  textArea.value = text;
  textArea.style.position = 'fixed';
  textArea.style.left = '-999999px';
  document.body.appendChild(textArea);
  textArea.select();
  try {
    document.execCommand('copy');
    alert('Copied to clipboard!');
  } catch (err) {
    alert('Failed to copy. Please copy manually: ' + text);
  }
  document.body.removeChild(textArea);
};
```

**HTTP Compatibility:** Handles both HTTPS (navigator.clipboard) and HTTP (execCommand fallback)

**Token Management Features:**
- Active tokens table (name, created, expires, last used, revoke button)
- Revoked tokens (collapsed, read-only view)
- Revoke endpoint: POST `/api/projects/${projectId}/tokens/${tokenId}/revoke`

### Middleware Configuration

**File:** `/Users/draco/projects/AI_HUB/apps/web/middleware.ts` (line 14)

```typescript
const publicApiPrefixes = ['/api/auth/', '/api/agent-auth/'];
```

**Configuration:** Allows `/api/agent-auth/` routes to bypass user authentication (MCP server needs to validate tokens independently)

---

## 4. Recommended Documentation Locations

### PRIMARY DELIVERABLE

**File:** `/Users/draco/projects/AI_HUB/docs/guides/mcp-authentication-setup.md` (NEW)

**Target Audience:** End users setting up agents for first time

**Content Outline:**
1. Introduction (200 words)
   - What are bearer tokens?
   - Why authentication?
   - Security model overview

2. Prerequisites (100 words)
   - Logged into ProjectPulse
   - Project created
   - Agent installed (Claude Code, Factory Droid, etc.)

3. Step-by-Step Token Generation (600 words)
   - Navigate to Project Settings
     * Log in at http://192.168.1.15:3000
     * Go to /app (user dashboard)
     * Click project
     * Click Settings (or /projects/[id]/settings)
   - Generate Token
     * Find "Agent Tokens" section
     * Click "Generate New Token"
     * Enter name (e.g., "Claude Code - Dev Laptop")
     * Set expiry (30 days default)
     * Click Generate
     * **CRITICAL:** Copy token immediately (one-time display)
   - Store Token Securely
     * Use password manager
     * Never commit to git
     * Treat like a password

4. Agent Configuration (500 words)
   - Claude Code Example
     ```json
     {
       "mcpServers": {
         "projectpulse": {
           "type": "http",
           "url": "http://192.168.1.15:3001/mcp",
           "headers": {
             "Authorization": "Bearer <your_token_here>"
           }
         }
       }
     }
     ```
   - Factory Droid Example (similar)
   - Generic HTTP Pattern

5. Testing & Validation (300 words)
   - Health check: `curl http://192.168.1.15:3001/health`
   - MCP initialize request
   - Tool list verification
   - First tool invocation

6. Token Management (400 words)
   - Viewing active tokens
   - Monitoring last used
   - Revoking tokens
   - Rotation strategy (security best practice)

7. Security Best Practices (400 words)
   - Use descriptive names
   - Set expiration dates
   - Revoke unused tokens
   - One token per agent/device
   - Monitor "Last Used" column
   - Rotate tokens periodically

8. Troubleshooting (600 words)
   - 401 Unauthorized errors
     * Missing Authorization header
     * Invalid token format
     * Expired token
     * Revoked token
   - Configuration issues
     * Wrong URL
     * Typo in token
     * Missing "Bearer " prefix
   - Server connectivity
     * Health check fails
     * Network issues
     * Docker not running

**Total Estimated Length:** 2500-3000 words

**Format:**
- Markdown with code blocks
- Clear section headers
- Step-by-step instructions
- Troubleshooting tables
- Security callouts

---

### SECONDARY DELIVERABLES

#### Update: docs/MCP_QUICK_START_v2.md

**Location:** After line 33 ("### 2. Generate a Project Token")

**Current Content (lines 41-50):**
```markdown
### 2. Generate a Project Token

Before any agent can call ProjectPulse tools, it must authenticate with a **project-scoped bearer token**:

1. Open ProjectPulse at `http://192.168.1.15:3000` and log in.
2. Navigate to your project Settings page: `/projects/[id]/settings`.
3. In the **Agent Tokens** section, click **"Generate New Token"**.
4. Choose a descriptive name (for the agent) and expiry, then generate.
5. **Copy the token immediately** – it will only be shown once.
6. Treat this token like a password; store it securely.

You will use this token in an `Authorization: Bearer <token>` header from each agent.
```

**Recommended Addition (after line 50):**
```markdown

**Detailed Token Generation Walkthrough:**

For complete step-by-step instructions with screenshots and troubleshooting, see the [MCP Authentication Setup Guide](../guides/mcp-authentication-setup.md).

**Quick Token Security Checklist:**
- ✅ Use descriptive token names (e.g., "Claude Code - MacBook Pro")
- ✅ Set expiration dates (30-90 days recommended)
- ✅ Copy token immediately (one-time display)
- ✅ Store in password manager (never in git)
- ✅ Revoke unused tokens regularly
- ❌ Never share tokens between agents/devices
- ❌ Never commit tokens to version control
```

**Estimated Addition:** 200 words

---

#### Update: docs/features/mcp-multi-agent-setup.md

**Location:** Expand section "### 2. Generate a Project Token" (currently lines 41-50)

**Current Content:**
```markdown
### 2. Generate a Project Token

Before any agent can call ProjectPulse tools, it must authenticate with a **project-scoped bearer token**:

1. Open ProjectPulse at `http://192.168.1.15:3000` and log in.
2. Navigate to your project Settings page: `/projects/[id]/settings`.
3. In the **Agent Tokens** section, click **"Generate New Token"**.
4. Choose a descriptive name (for the agent) and expiry, then generate.
5. **Copy the token immediately** – it will only be shown once.
6. Treat this token like a password; store it securely.

You will use this token in an `Authorization: Bearer <token>` header from each agent.
```

**Recommended Expansion:**
```markdown

**Token Management Tips:**

After generating your token:

1. **Store Securely:**
   - Use a password manager (1Password, LastPass, Bitwarden)
   - Add entry: "ProjectPulse MCP Token - [Agent Name]"
   - Store token value, creation date, expiry date

2. **Monitor Usage:**
   - Check "Last Used" column in Settings regularly
   - Revoke tokens that haven't been used in 30+ days
   - Rotate tokens every 90 days (security best practice)

3. **Troubleshooting:**
   - If you see 401 errors, verify token hasn't expired
   - Check token is correctly copied (no extra spaces)
   - Ensure "Bearer " prefix is included in header

**For Complete Documentation:**
See [MCP Authentication Setup Guide](../guides/mcp-authentication-setup.md) for detailed workflow, security best practices, and troubleshooting.
```

**Estimated Addition:** 300 words

---

#### Update: docs/README.md

**Location:** Add to documentation index (after "User Guides" section)

**Current Structure (lines 44-100):**
```markdown
### Product & Requirements
1. **[01-PRD.md](01-PRD.md)** - Product Requirements Document
2. **[02-SRS.md](02-SRS.md)** - System Requirements Specification

### Architecture & Design
3. **[03-Architecture.md](03-Architecture.md)** - System Architecture
...
```

**Recommended Addition (after line ~80, in "Operations & Implementation" or new "Setup Guides" section):**
```markdown

### Setup & Configuration Guides

- **[MCP Authentication Setup](guides/mcp-authentication-setup.md)** - Generate and configure bearer tokens for agent MCP access
- **[MCP Quick Start](MCP_QUICK_START_v2.md)** - Quick setup for Claude Code, Windsurf, Cascade
- **[Multi-Agent Setup](features/mcp-multi-agent-setup.md)** - Configure multiple agents with validated configurations
- **[Onboarding User Guide](guides/onboarding-user-guide.md)** - Complete project onboarding workflow
```

**Estimated Addition:** 100 words

---

### OPTIONAL DELIVERABLE

#### Wiki Page Creation

**Title:** "MCP Authentication Setup"

**Path:** `/setup/mcp-authentication`

**Category:** "Setup" or "Getting Started"

**Tags:** `["mcp", "authentication", "setup", "tokens", "agents", "bearer", "security"]`

**Excerpt:**
```
Learn how to generate and configure project-scoped bearer tokens for AI agent MCP access. Includes step-by-step token generation, agent configuration examples, and security best practices.
```

**Content Strategy:**

**Option A: Summary + Link** (Recommended)
```markdown
# MCP Authentication Setup

Quick guide to setting up bearer token authentication for AI agents accessing ProjectPulse MCP tools.

## Overview

ProjectPulse uses project-scoped bearer tokens for MCP authentication. Each agent needs a unique token to access MCP tools securely.

## Quick Start

1. Navigate to **Project Settings** (`/projects/[id]/settings`)
2. Click **"Generate New Token"** in Agent Tokens section
3. Enter descriptive name and expiry
4. **Copy token immediately** (one-time display)
5. Configure agent with token
6. Test connection

## Complete Guide

For detailed step-by-step instructions, screenshots, troubleshooting, and security best practices, see:

**[MCP Authentication Setup Guide](/docs/guides/mcp-authentication-setup)**

## Common Issues

| Issue | Solution |
|-------|----------|
| 401 Unauthorized | Check token is valid and not expired |
| Token expired | Generate new token in Settings |
| Missing header | Verify `Authorization: Bearer <token>` format |
| Connection refused | Check MCP server is running (health endpoint) |

## Security Checklist

- ✅ Use descriptive token names
- ✅ Set expiration dates (30-90 days)
- ✅ Store tokens in password manager
- ✅ Revoke unused tokens regularly
- ✅ One token per agent/device
- ❌ Never commit tokens to git
- ❌ Never share tokens

## See Also

- [MCP Quick Start Guide](/docs/MCP_QUICK_START_v2)
- [Multi-Agent Setup](/docs/features/mcp-multi-agent-setup)
- [Project Settings Documentation](/docs/features/project-settings)
```

**Option B: Full Content** (Self-Contained)
- Embed complete mcp-authentication-setup.md content
- Maintain single source of truth in wiki
- Requires keeping markdown guide in sync

**Recommended:** Option A (summary + link) keeps wiki concise while providing comprehensive external guide.

**Creation Method:**

1. **Manual via Web UI:**
   - Navigate to `/wiki/new?project=1`
   - Fill in form
   - Submit

2. **MCP Tool Invocation:**
   ```typescript
   {
     "name": "wiki.create",
     "arguments": {
       "projectId": 1,
       "title": "MCP Authentication Setup",
       "content": "...", // Full markdown content
       "category": "Setup",
       "excerpt": "Learn how to generate and configure project-scoped bearer tokens...",
       "tags": ["mcp", "authentication", "setup", "tokens", "agents"]
     }
   }
   ```

3. **Database Seed (NOT Recommended for Production Docs):**
   - Add to `apps/web/prisma/seed.ts`
   - Requires database reset to apply

**Recommendation:** Create manually via web UI OR use MCP tool after markdown guide is complete.

---

## 5. Example Content Structure

### Full Guide Outline (docs/guides/mcp-authentication-setup.md)

```markdown
# MCP Authentication Setup Guide

**Last Updated:** 2025-11-22  
**Sprint:** 9  
**Audience:** End users setting up AI agents

---

## Table of Contents

1. [Introduction](#introduction)
2. [Prerequisites](#prerequisites)
3. [Generating Your First Token](#generating-your-first-token)
4. [Configuring Your Agent](#configuring-your-agent)
5. [Testing Your Setup](#testing-your-setup)
6. [Token Management](#token-management)
7. [Security Best Practices](#security-best-practices)
8. [Troubleshooting](#troubleshooting)
9. [FAQ](#faq)

---

## Introduction

### What are Bearer Tokens?

Bearer tokens are opaque credentials used to authenticate AI agents with the ProjectPulse MCP server. Each token is:

- **Project-scoped:** Only works for a single project
- **Time-limited:** Optional expiration dates (30-90 days recommended)
- **Revocable:** Can be invalidated at any time
- **One-way hashed:** Plaintext never stored in database (bcrypt)

### Why Authentication?

ProjectPulse uses bearer token authentication to:

1. **Prevent unauthorized access** to project data
2. **Track agent usage** (last used timestamps)
3. **Enable multi-tenancy** (multiple projects per instance)
4. **Support token rotation** (security best practice)

### Security Model

```
Agent (Claude Code, etc.)
  ↓ HTTP Request with Authorization: Bearer <token>
MCP Server (port 3001)
  ↓ POST /api/agent-auth/validate { token }
Web App (port 3000)
  ↓ bcrypt.compare(token, tokenHash)
Database (ProjectToken table)
  ↓ Return { projectId, tokenId, name }
MCP Server
  ↓ Attach agentAuth to request
Tool Execution
```

**Key Points:**
- Plaintext token only shown ONCE during generation
- Token hash stored in database (bcrypt)
- Validation happens on every MCP request
- Invalid/expired/revoked tokens return 401 Unauthorized

---

## Prerequisites

Before generating a token, ensure you have:

- ✅ **ProjectPulse instance running** at `http://192.168.1.15:3000`
- ✅ **User account** (logged in)
- ✅ **Project created** (or know project ID)
- ✅ **Agent installed** (Claude Code, Factory Droid, Windsurf, etc.)

**Not set up yet?**
- See [Onboarding User Guide](onboarding-user-guide.md) for complete project setup
- See [MCP Quick Start](../MCP_QUICK_START_v2.md) for agent installation

---

## Generating Your First Token

### Step 1: Navigate to Project Settings

1. Open ProjectPulse: `http://192.168.1.15:3000`
2. Log in with your credentials
3. Click **"Go to Dashboard"** (or navigate to `/app`)
4. Click on your project card
5. Click **"Settings"** tab (or navigate to `/projects/[id]/settings`)

**URL Format:** `/projects/1/settings` (replace `1` with your project ID)

### Step 2: Open Token Generation Modal

In the **Agent Tokens** section:

1. Locate the "Agent Tokens" card (first section on page)
2. Click **"Generate New Token"** button (coral-colored, top-right)
3. Modal will appear with token generation form

### Step 3: Fill Token Details

**Token Name** (required):
- Use descriptive name identifying agent and device
- Examples:
  - ✅ "Claude Code - MacBook Pro"
  - ✅ "Factory Droid - Dev Laptop"
  - ✅ "Windsurf - Work Desktop"
  - ❌ "Token 1" (too generic)
  - ❌ "My Token" (not descriptive)

**Expiry Days** (optional, default 30):
- Recommended: 30-90 days
- Never expires: Leave blank or set to 0
- Security best practice: Use expiration dates

### Step 4: Generate and Copy Token

1. Click **"Generate"** button
2. **CRITICAL:** Token plaintext displayed ONCE in success modal
3. **Immediately copy** token using:
   - "Copy to Clipboard" button (HTTPS)
   - Manual selection + Cmd+C (HTTP fallback)
4. **Store token securely** (see Security Best Practices)

**⚠️ WARNING:** Token will NEVER be shown again after closing modal. If lost, you must generate a new token.

### Step 5: Verify Token Created

After closing modal:

1. Token appears in "Active Tokens" table
2. Verify name and creation date
3. Note expiration date (if set)

---

## Configuring Your Agent

### Claude Code

**Method 1: JSON Config** (Recommended)

Edit `~/.claude.json`:

```json
{
  "mcpServers": {
    "projectpulse": {
      "type": "http",
      "url": "http://192.168.1.15:3001/mcp",
      "headers": {
        "Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." 
      }
    }
  }
}
```

Replace `eyJhbG...` with your actual token.

**Method 2: CLI Command**

```bash
# Edit config manually first, then:
claude mcp reload projectpulse
```

**Restart:** Quit Claude Code completely (Cmd+Q) and reopen.

### Factory Droid

Edit `~/.factory/mcp.json`:

```json
{
  "mcpServers": {
    "projectpulse": {
      "type": "http",
      "url": "http://192.168.1.15:3001/mcp",
      "headers": {
        "Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      },
      "disabled": false
    }
  }
}
```

**Restart:** Exit current session (Ctrl+C) and run `droid`.

### Generic HTTP Client Pattern

All MCP-compliant clients support HTTP transport with custom headers:

```json
{
  "mcpServers": {
    "projectpulse": {
      "type": "http",
      "url": "http://192.168.1.15:3001/mcp",
      "headers": {
        "Authorization": "Bearer <your_token_here>"
      }
    }
  }
}
```

**Key Requirements:**
- Header name MUST be `Authorization`
- Value MUST start with `Bearer ` (note space)
- Token MUST be complete (no truncation)

**For other agents:**
- See [Multi-Agent Setup Guide](../features/mcp-multi-agent-setup.md)

---

## Testing Your Setup

### Step 1: Health Check

```bash
curl http://192.168.1.15:3001/health
```

**Expected Response:**
```json
{
  "status": "healthy",
  "version": "0.1.0",
  "transport": "http",
  "toolCount": 40,
  "endpoint": "/mcp"
}
```

**If this fails:** MCP server is not running. Check Docker:
```bash
docker ps --filter "name=projectpulse-mcp-cloud"
```

### Step 2: MCP Initialize Request

```bash
curl -X POST http://192.168.1.15:3001/mcp \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <your_token>" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "initialize",
    "params": {
      "protocolVersion": "2024-11-05",
      "capabilities": {},
      "clientInfo": {"name": "test-client", "version": "1.0"}
    }
  }'
```

**Expected:** JSON response with server capabilities (no 401 error).

### Step 3: Tool List Verification

```bash
curl -X POST http://192.168.1.15:3001/mcp \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <your_token>" \
  -d '{
    "jsonrpc": "2.0",
    "id": 2,
    "method": "tools/list",
    "params": {}
  }'
```

**Expected:** JSON response with 40 tools.

### Step 4: Agent Invocation

In your agent (Claude Code, etc.):

```
List all available MCP tools
```

**Expected:** Agent calls `tools/list` and shows 40 ProjectPulse tools.

**If you see 401 errors:** See Troubleshooting section.

---

## Token Management

### Viewing Active Tokens

Navigate to `/projects/[id]/settings` and scroll to "Agent Tokens" section.

**Active Tokens Table:**
| Name | Created | Expires | Last Used | Actions |
|------|---------|---------|-----------|---------|
| Claude Code - MacBook Pro | Nov 22, 2025 | Dec 22, 2025 | Nov 22, 2025 | [Revoke] |
| Factory Droid - Dev Laptop | Nov 20, 2025 | Never | Nov 21, 2025 | [Revoke] |

**Columns:**
- **Name:** Descriptive label you provided
- **Created:** Generation timestamp
- **Expires:** Expiration date (or "Never")
- **Last Used:** Last successful MCP request (or "Never")
- **Actions:** Revoke button

### Monitoring Usage

**Last Used Column:** Updates automatically when agent makes authenticated MCP request.

**Use Cases:**
- Identify inactive tokens (revoke if not used in 30+ days)
- Verify agent is connecting successfully (timestamp updates)
- Detect token misuse (unexpected usage patterns)

### Revoking Tokens

**When to Revoke:**
- Token compromised or leaked
- Agent no longer in use
- Device lost or stolen
- Token not used in 30+ days
- Replacing with rotated token

**How to Revoke:**
1. Navigate to `/projects/[id]/settings`
2. Find token in "Active Tokens" table
3. Click trash icon (Revoke button)
4. Confirm revocation in popup
5. Token moved to "Revoked Tokens" (collapsed section)

**Effect:** Token IMMEDIATELY invalid. Agent will receive 401 errors on next request.

### Token Rotation Strategy

**Recommended:** Rotate tokens every 90 days (security best practice).

**Rotation Workflow:**
1. Generate new token (new name: "Claude Code - MacBook Pro v2")
2. Update agent configuration with new token
3. Test agent connection (verify working)
4. Revoke old token
5. Schedule next rotation in 90 days

**Automation:** Set calendar reminder for rotation.

---

## Security Best Practices

### 1. Use Descriptive Names

✅ **Good:**
- "Claude Code - MacBook Pro Dev"
- "Factory Droid - Home Office Desktop"
- "Windsurf - Work Laptop 2024"

❌ **Bad:**
- "Token 1"
- "My Token"
- "Test"

**Why:** Makes it easy to identify and revoke specific tokens.

### 2. Set Expiration Dates

✅ **Recommended:** 30-90 days

❌ **Avoid:** Never expires (unless absolutely necessary)

**Why:** Limits exposure window if token leaked.

### 3. Store Tokens in Password Manager

✅ **Use:**
- 1Password
- LastPass
- Bitwarden
- macOS Keychain (secure notes)

❌ **Never:**
- Plain text files
- Git repositories
- Unencrypted cloud storage
- Email

**Entry Format:**
```
Title: ProjectPulse MCP Token - Claude Code MacBook Pro
Username: project-1-claude-code
Password: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
URL: http://192.168.1.15:3001/mcp
Notes:
- Created: 2025-11-22
- Expires: 2025-12-22
- Rotate on: 2026-02-22
```

### 4. Revoke Unused Tokens

**Monthly Review:**
1. Check "Last Used" column
2. Revoke tokens not used in 30+ days
3. Investigate unexpected usage

### 5. One Token Per Agent/Device

❌ **Never:** Share tokens across multiple agents

✅ **Always:** Generate unique token for each:
- Agent type (Claude Code, Factory Droid)
- Device (MacBook, Desktop, Laptop)
- Environment (Dev, Staging)

**Why:** Enables granular revocation (lost device, decommissioned agent).

### 6. Monitor "Last Used" Column

**Weekly Check:**
- Verify expected agents are connecting
- Detect unauthorized usage
- Identify stale tokens

### 7. Rotate Tokens Periodically

**Schedule:** Every 90 days (calendar reminder)

**Process:**
1. Generate new token
2. Update agent config
3. Test connection
4. Revoke old token

### 8. Never Commit Tokens to Git

**Git Configuration:**

Add to `.gitignore`:
```
# MCP Tokens
.claude.json
.factory/mcp.json
mcp-token.txt
**/mcp-config*.json
```

**Pre-Commit Hook:**
```bash
# Check for token patterns
if git diff --cached | grep -i "bearer ey"; then
  echo "ERROR: Potential token in commit!"
  exit 1
fi
```

### 9. Secure Agent Configuration Files

**File Permissions:**
```bash
chmod 600 ~/.claude.json
chmod 600 ~/.factory/mcp.json
```

**Ownership:**
```bash
chown $USER:$USER ~/.claude.json
```

---

## Troubleshooting

### 401 Unauthorized Errors

**Symptoms:**
- Agent cannot connect to MCP server
- MCP requests fail with JSON-RPC error code -32001
- Error message: "Unauthorized: Invalid or expired token"

**Causes & Solutions:**

#### 1. Missing Authorization Header

**Cause:** Agent config missing `headers` section.

**Check:**
```json
// ❌ Wrong
{
  "mcpServers": {
    "projectpulse": {
      "type": "http",
      "url": "http://192.168.1.15:3001/mcp"
    }
  }
}

// ✅ Correct
{
  "mcpServers": {
    "projectpulse": {
      "type": "http",
      "url": "http://192.168.1.15:3001/mcp",
      "headers": {
        "Authorization": "Bearer <token>"
      }
    }
  }
}
```

**Fix:** Add `headers` section with Authorization.

#### 2. Invalid Token Format

**Cause:** Missing "Bearer " prefix or token truncated.

**Check:**
```bash
# Test with curl
curl -X POST http://192.168.1.15:3001/mcp \
  -H "Authorization: Bearer <your_token>" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'
```

**Common Mistakes:**
- ❌ `"Authorization": "<token>"` (missing "Bearer ")
- ❌ `"Authorization": "bearer <token>"` (lowercase "bearer")
- ❌ `"Authorization": "Bearer  <token>"` (extra space)
- ✅ `"Authorization": "Bearer <token>"` (correct)

#### 3. Token Expired

**Cause:** Token expiration date passed.

**Check:** Navigate to `/projects/[id]/settings` and verify "Expires" column.

**Fix:**
1. Generate new token
2. Update agent config
3. Revoke old token

#### 4. Token Revoked

**Cause:** Token was manually revoked.

**Check:** Look for token in "Revoked Tokens" section (collapsed).

**Fix:** Generate new token.

#### 5. Wrong Project Token

**Cause:** Using token from different project.

**Check:** Verify `projectId` in Settings URL matches your project.

**Fix:** Generate token for correct project.

### Connection Refused Errors

**Symptoms:**
- Agent cannot reach MCP server
- Error: "ECONNREFUSED" or "Connection refused"

**Causes & Solutions:**

#### 1. MCP Server Not Running

**Check:**
```bash
curl http://192.168.1.15:3001/health
```

**Expected:** JSON response with "status": "healthy"

**If fails:**
```bash
# Check Docker container
docker ps --filter "name=projectpulse-mcp-cloud"

# Restart if needed
docker restart projectpulse-mcp-cloud

# Check logs
docker logs projectpulse-mcp-cloud --tail 50
```

#### 2. Wrong URL

**Common Mistakes:**
- ❌ `http://192.168.1.15:3000/mcp` (port 3000 = web app, not MCP)
- ❌ `http://192.168.1.15:3001` (missing /mcp path)
- ❌ `https://192.168.1.15:3001/mcp` (HTTPS instead of HTTP)
- ✅ `http://192.168.1.15:3001/mcp` (correct)

#### 3. Network Issues

**Check:**
```bash
ping 192.168.1.15
# Should have <10ms latency on local network
```

**If fails:** Verify Mac mini is on and connected to network.

### Configuration Issues

#### Agent Not Loading Tools

**Symptoms:**
- Agent starts successfully
- No MCP tools appear in tool list
- No errors shown

**Causes:**
1. **Config file location wrong**
   - Check agent-specific config path (see Multi-Agent Setup)
2. **JSON syntax error**
   - Validate JSON: `cat ~/.claude.json | jq .`
3. **Agent not restarted**
   - Fully quit agent (Cmd+Q) and reopen

#### Tools List Empty

**Symptoms:**
- Agent connects successfully
- `tools/list` returns empty array or error

**Check:**
```bash
# Verify tools registered
docker logs projectpulse-mcp-cloud | grep "Tools registered"
# Should show: [INFO] Tools registered {"count":40}
```

**Fix:**
```bash
docker restart projectpulse-mcp-cloud
```

### Token Copying Issues

#### Clipboard Failed (HTTP Context)

**Symptoms:**
- "Copy to Clipboard" button fails
- Navigator.clipboard not available

**Cause:** HTTP context (not HTTPS) blocks clipboard API.

**Solution:** Fallback automatically triggered:
1. TextArea created
2. Token selected
3. execCommand('copy') executed
4. Manual copy if fallback fails

**Alternative:** Manually select and copy token from modal.

---

## FAQ

### Q: Can I reuse the same token for multiple agents?

**A:** Technically yes, but NOT recommended. Generate unique tokens per agent/device for:
- Granular revocation (lost device)
- Usage tracking (which agent made requests)
- Security best practice (limit blast radius)

### Q: What happens if I lose a token?

**A:** Generate a new token. Old token cannot be recovered (plaintext never stored).

Workflow:
1. Generate new token
2. Update agent config
3. Revoke old token (if it still exists)

### Q: How do I rotate tokens?

**A:** See "Token Rotation Strategy" in Token Management section.

Summary:
1. Generate new token (new name)
2. Update agent config
3. Test connection
4. Revoke old token

### Q: Can tokens be used from different machines?

**A:** Yes, tokens work from any machine with network access to MCP server. However, generate unique tokens per machine for security.

### Q: Do tokens expire automatically?

**A:** Yes, if you set an expiration date during generation. Otherwise, tokens never expire (manual revocation required).

### Q: What's the token format?

**A:** Opaque string (bcrypt hash). Format is implementation detail and may change.

### Q: Can I see token plaintext after generation?

**A:** NO. Token plaintext shown ONLY ONCE during generation. If lost, generate new token.

### Q: How are tokens validated?

**A:** Every MCP request includes `Authorization: Bearer <token>` header. MCP server calls web app `/api/agent-auth/validate` endpoint, which uses bcrypt to compare token with stored hash.

### Q: Can I use API keys instead of bearer tokens?

**A:** No. ProjectPulse uses bearer token authentication exclusively for MCP access.

---

## Additional Resources

- [MCP Quick Start Guide](../MCP_QUICK_START_v2.md) - Quick agent setup
- [Multi-Agent Setup](../features/mcp-multi-agent-setup.md) - Validated agent configurations
- [MCP Architecture](../MCP_ARCHITECTURE.md) - Technical implementation details
- [Project Settings Documentation](../features/project-settings.md) - All settings options

---

**Last Updated:** 2025-11-22  
**Sprint:** 9  
**Status:** Production Ready
```

---

## 6. Implementation Checklist

### Phase 1: Primary Documentation (CRITICAL)

- [ ] Create `/Users/draco/projects/AI_HUB/docs/guides/mcp-authentication-setup.md`
  - [ ] Write Introduction section (200 words)
  - [ ] Write Prerequisites section (100 words)
  - [ ] Write Step-by-Step Token Generation (600 words)
  - [ ] Write Agent Configuration (500 words)
  - [ ] Write Testing & Validation (300 words)
  - [ ] Write Token Management (400 words)
  - [ ] Write Security Best Practices (400 words)
  - [ ] Write Troubleshooting (600 words)
  - [ ] Write FAQ (300 words)
  - [ ] Add code examples and formatting
  - [ ] Review for accuracy and completeness

### Phase 2: Update Existing Docs (IMPORTANT)

- [ ] Update `/Users/draco/projects/AI_HUB/docs/MCP_QUICK_START_v2.md`
  - [ ] Add detailed token generation section after line 50
  - [ ] Add security checklist
  - [ ] Add link to full guide (~200 words)

- [ ] Update `/Users/draco/projects/AI_HUB/docs/features/mcp-multi-agent-setup.md`
  - [ ] Expand token generation section (lines 41-50)
  - [ ] Add token management tips
  - [ ] Add troubleshooting subsection
  - [ ] Add link to full guide (~300 words)

- [ ] Update `/Users/draco/projects/AI_HUB/docs/README.md`
  - [ ] Add "Setup & Configuration Guides" section
  - [ ] Add link to mcp-authentication-setup.md
  - [ ] Update table of contents (~100 words)

### Phase 3: Wiki Page (OPTIONAL)

- [ ] Create wiki page via web UI
  - [ ] Navigate to `/wiki/new?project=1`
  - [ ] Title: "MCP Authentication Setup"
  - [ ] Path: `/setup/mcp-authentication`
  - [ ] Category: "Setup"
  - [ ] Tags: mcp, authentication, setup, tokens, agents, bearer, security
  - [ ] Content: Summary + link to full guide
  - [ ] Submit and verify

OR

- [ ] Create wiki page via MCP tool
  - [ ] Use `wiki.create` tool
  - [ ] projectId: 1
  - [ ] Same metadata as above
  - [ ] Verify creation in database

### Phase 4: Validation (CRITICAL)

- [ ] Technical review
  - [ ] Verify all URLs are correct (192.168.1.15 ports)
  - [ ] Test all code examples
  - [ ] Verify all file paths
  - [ ] Check cross-references between docs

- [ ] User testing
  - [ ] Follow guide end-to-end as new user
  - [ ] Verify all screenshots match UI
  - [ ] Test troubleshooting steps
  - [ ] Verify all links work

- [ ] Documentation review
  - [ ] Check grammar and spelling
  - [ ] Verify markdown formatting
  - [ ] Ensure consistent tone and style
  - [ ] Verify table of contents

### Phase 5: Integration (FINAL)

- [ ] Commit documentation changes
  - [ ] Commit message: "docs: Add comprehensive MCP authentication setup guide"
  - [ ] Include all file changes in single commit

- [ ] Update changelog (if exists)

- [ ] Announce documentation in team channels

- [ ] Add to project wiki index page (if exists)

---

## 7. Estimated Effort

**Phase 1 (Primary Guide):** 4-6 hours
- Research: 0.5 hours (COMPLETE)
- Writing: 3-4 hours
- Formatting: 0.5 hours
- Review: 0.5-1 hour

**Phase 2 (Updates):** 1-2 hours
- MCP_QUICK_START update: 30 min
- mcp-multi-agent-setup update: 45 min
- README update: 15 min
- Review: 30 min

**Phase 3 (Wiki Page):** 30 min - 1 hour
- Content creation: 15 min
- Manual creation via UI: 15 min
- OR MCP tool invocation: 5 min
- Verification: 15 min

**Phase 4 (Validation):** 2-3 hours
- Technical review: 1 hour
- User testing: 1-1.5 hours
- Documentation review: 30 min

**Phase 5 (Integration):** 30 min
- Git commit: 10 min
- Changelog: 10 min
- Announcements: 10 min

**TOTAL ESTIMATED TIME:** 8-12 hours (1-1.5 days)

**PRIORITY:** CRITICAL (blocking user onboarding)

---

## 8. Success Metrics

### Documentation Quality

- [ ] All code examples tested and working
- [ ] All screenshots match current UI
- [ ] All links functional
- [ ] No grammar/spelling errors
- [ ] Consistent formatting

### User Success

- [ ] New user can generate token in <5 minutes
- [ ] New user can configure agent in <10 minutes
- [ ] Troubleshooting section resolves 90%+ of issues
- [ ] Security best practices clearly understood

### Search & Discovery

- [ ] Guide appears in docs index (README.md)
- [ ] Guide discoverable via wiki search (if wiki page created)
- [ ] Cross-referenced from other MCP docs

### Maintenance

- [ ] Documentation version tracked
- [ ] Update process documented
- [ ] Screenshot update process defined
- [ ] Regular review schedule established

---

## 9. Next Steps

### Immediate (Sprint 9 Completion)

1. ✅ **Research Complete** - This document
2. ⏳ **Create Primary Guide** - docs/guides/mcp-authentication-setup.md
3. ⏳ **Update Existing Docs** - MCP_QUICK_START, mcp-multi-agent-setup, README
4. ⏳ **Validation** - Technical and user testing
5. ⏳ **Commit Changes** - Single commit with all documentation

### Short-Term (Sprint 10)

1. Create wiki page (database-backed)
2. Add screenshots to guide
3. Create video walkthrough (optional)
4. User acceptance testing

### Long-Term (Future)

1. Maintain guide as implementation evolves
2. Update screenshots with UI changes
3. Add advanced configuration examples
4. Integrate with onboarding flow (guided setup)

---

## 10. Conclusion

**Status:** Research complete, ready for implementation.

**Critical Gap:** MCP authentication is fully implemented but completely undocumented for end users.

**Impact:** Users cannot onboard without this documentation (blocks adoption).

**Recommendation:** Prioritize creating comprehensive authentication guide (docs/guides/mcp-authentication-setup.md) and updating cross-referenced docs.

**Estimated Effort:** 8-12 hours (1-1.5 days)

**Priority:** CRITICAL

---

**Research Report Complete**
**Date:** 2025-11-22
**Researcher:** Claude Code (Sequential Thinking)
**Status:** Ready for Implementation

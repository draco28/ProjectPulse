# Workflow 07 — MCP Tool Creation

Steps:

1. Define tool intent, inputs/outputs (Zod)
2. Route calls to Next.js API endpoints only
3. Implement resources with pagination/filters
4. Unit tests for tool; E2E against local dev API
5. Verification Gate (Workflow 11)

Acceptance:

- All calls via API; validated params; tests ≥80%

References: rules/08-mcp-integration.md

# 06 Data-Driven and Configuration

- No hardcoded business values (statuses, priorities, modules, colors)
- Use configuration tables or typed enums
- Prefer Prisma enums and lookup tables; seed canonical values
- Parameterize queries and use select/include for performance

References: AGENTS.md Golden Rules [R-DATA-001]

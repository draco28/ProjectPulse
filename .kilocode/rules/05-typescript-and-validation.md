# 05 TypeScript and Validation

- Strict TS everywhere; no `any`
- Explicit return types for functions
- Zod schemas for request/response/error at API boundaries
- Narrow types from IO schemas into service/domain types
- Server Components by default; client components only for interactivity

References: AGENTS.md Technical Standards, CLAUDE.md Skills and Context Loading

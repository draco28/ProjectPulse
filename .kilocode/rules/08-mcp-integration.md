# 08 MCP Integration

- MCP tools call Next.js API endpoints, not the database directly (R-MCP-001)
- Define tool intent, inputs (Zod), outputs (Zod)
- Support pagination and filters in resources
- Provide unit tests (tool level) and E2E against local dev API

References: docs/03-MCP-SPECIFICATION.md, .agent/system/mcp-tools-guide.md

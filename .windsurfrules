# ProjectPulse Windsurf Rules - Cascade Configuration

**Version**: 2.0 (Context-Optimized)  
**Last Updated**: 2025-10-28

This file mirrors the authoritative workflow from `CLAUDE.md` so that Windsurf enforces the same rules and protocols.

---

## Quick Start

Work with Cascade in Windsurf the same way you would with Claude Code:

```
"Implement POST /api/issues endpoint"
"Write tests for the search API"
"Debug the authentication flow"
```

---

## 🚨 CRITICAL: Pre-Work Checklist

**BEFORE starting ANY coding work:**

### 1. Port Configuration

```bash
pnpm dev
# ✅ MUST show: "ready started server on 0.0.0.0:3000"
# ❌ WRONG: "ready started server on 0.0.0.0:3002"
```

See `.agent/sops/port-troubleshooting.md` for recovery steps.

### 2. Git Branch

```bash
git branch
# ✅ MUST be on feature branch (NOT master!)
# If on master:
git checkout master && git pull origin master
git checkout -b feature/your-feature
```

See `.agent/sops/git-workflow.md` for the full SOP.

---

## 🚨 CRITICAL: Mandatory Session Protocol

Every session in Windsurf MUST follow the same protocol described in `CLAUDE.md`.

**Why this exists:** Cascade reads instructions but will not follow them unless confirmations are requested explicitly.

**How it works:**

1. Copy-paste the starter prompt at session start.
2. Cascade must complete and confirm all five protocol steps.
3. Missing confirmation = workflow violation (call it out immediately).

**📋 Full Protocol:** `.agent/MANDATORY_SESSION_PROTOCOL.md`  
**🚀 Quick Start Guide:** `SESSION_START_QUICK_GUIDE.md`

---

## Session Start Pattern (REQUIRED PROTOCOL)

### Starter Prompt (Copy-Paste This)

```
MANDATORY PROTOCOL - Read .agent/MANDATORY_SESSION_PROTOCOL.md and follow ALL steps.

Current phase: [copy from STATUS.md]
Requirements: [copy from docs/13-Project-Plan.md]

ENFORCE:
- ✅ Step 1: Initialize session
- ✅ Step 2: Save plan BEFORE code
- ✅ Step 3: Consult experts
- ✅ Step 4: Checkpoints every 15K tokens
- ✅ Step 5: Post-completion workflow

Confirm each step explicitly. If you skip ANY step, I will stop you.

Proceed with [phase name].
```

---

## What Cascade Must Do (Per Protocol)

### STEP 1: INITIALIZATION

- Read `STATUS.md` and `docs/13-Project-Plan.md`.
- Create `.agent/task/current-session-[YYYYMMDD-HHMM].md`.
- **Confirm:** `✅ STEP 1 COMPLETE: Session initialized at [timestamp]`.

**Reading Path After Step 1**

- **Implementation phases:** `docs/03-Architecture.md` → `docs/04-Data-and-Model-Spec.md` → `docs/06-API/openapi.yaml`
- **Planning phases:** `docs/01-PRD.md` → `docs/02-SRS.md` → `docs/12-Backlog.md`

(See `docs/README.md` for full reading paths.)

### STEP 2: PLAN CREATION

- Create an implementation plan (use ExitPlanMode if helpful).
- Obtain user approval.
- **Immediately save** plan to `.agent/task/current-plan.md`.
- Create `.agent/task/current-todos.md`.
- **Confirm:** `✅ STEP 2 COMPLETE: Plan saved to current-plan.md, todos saved to current-todos.md`.

### STEP 3: EXPERT CONSULTATION

- Invoke `react-expert` for component architecture.
- Invoke `next-js-expert` for Server/Client and data fetching decisions.
- Invoke `prisma-expert` for database schema and query optimization.
- **Confirm:** `✅ STEP 3 COMPLETE: Consulted [expert-name] for [decision-topic]`.

**When experts are required**

- New component architectures or complex UI flows.
- Complex backend features or performance-sensitive areas.
- Any database schema changes or non-trivial Prisma queries.

**When experts are optional**

- Routine CRUD following established patterns.
- Minor UI updates that match existing conventions.
- Small refactors within an established module.

### STEP 4: PROGRESS CHECKPOINTS

- At 15K, 30K, 45K, 60K, 75K, 90K tokens:
  - Update `.agent/task/current-session.md`.
  - Update `.agent/task/current-todos.md`.
- **Confirm:** `✅ CHECKPOINT at [X]K tokens: Progress saved`.

### STEP 5: POST-COMPLETION

- Create a completion doc if the phase warrants it.
- Update `STATUS.md` and `docs/13-Project-Plan.md`.
- Invoke `synthesize-docs` if new patterns emerged.
- Invoke `map-system` if architecture changed.
- Commit documentation first, then code.
- **Confirm:** `✅ STEP 5 COMPLETE: All documentation updated and committed`.

---

## Skills and Context Loading

Cascade loads skills based on phase keywords:

| Phase Contains                | Skills to Load                                                          |
| ----------------------------- | ----------------------------------------------------------------------- |
| "API", "endpoint", "route"    | `.claude/skills/projectpulse/api-patterns.md`                           |
| "Component", "UI", "page"     | `.claude/skills/projectpulse/component-patterns.md`                     |
| "Database", "Prisma", "query" | `.claude/skills/projectpulse/database-patterns.md`                      |
| "Test", "testing", "coverage" | `.claude/skills/projectpulse/testing-patterns.md`                        |

---

## Sub-Agent Invocations

**Research agents (planning):**

- `explore-codebase` – identify existing patterns.
- `analyze-architecture` – trace data flows.

**Expert agents (protocol Step 3):**

- `react-expert` – component architecture.
- `next-js-expert` – server/client boundaries.
- `prisma-expert` – database modeling and queries.

**Documentation agents (protocol Step 5):**

- `synthesize-docs` – generate or extend SOPs.
- `map-system` – update architecture/system documentation.

---

## Context File Workflow (REQUIRED)

### At Session Start (Step 1)

Cascade must:

1. Create `.agent/task/current-session-[YYYYMMDD-HHMM].md`.
2. Document current phase, goals, and requirements derived from `STATUS.md`.
3. Update this file at every protocol checkpoint.

### When Invoking Sub-Agents (Step 3)

Cascade must:

1. Pass the context file (`.agent/task/current-session.md`) to the specialist.
2. Wait for the research/analysis report (saved under `.agent/task/`).
3. Read and incorporate each report before implementing.
4. Update the session file with implementation notes.

---

## Quick Reference

### Daily Checklist

```markdown
- [ ] pnpm dev shows port 3000
- [ ] localhost:3000 loads application
- [ ] On feature branch (not master)
- [ ] Read STATUS.md and docs/13-Project-Plan.md
- [ ] Check .agent/README.md for task context
```

### Common Tasks

- **Need API structure?** → `.agent/system/api-catalog.md`
- **Need database schema?** → `.agent/system/database-schema.md`
- **Port configuration broken?** → `.agent/sops/port-troubleshooting.md`
- **Creating a branch?** → `.agent/sops/git-workflow.md`
- **Deep codebase analysis?** → Use the Gemini CLI (`SIMPLE_GEMINI_WORKFLOW.md`)

### Integration Checklist

- [ ] MCP tools configured and working  
- [ ] Access to `.agent/` documentation verified  
- [ ] `pnpm dev` runs successfully  
- [ ] Actively working on the correct feature branch  
- [ ] `STATUS.md` and `docs/13-Project-Plan.md` reviewed

---

## Getting Help

**Documentation:**

1. `.agent/README.md` – Agent documentation index
2. `STATUS.md` – Current state
3. `docs/13-Project-Plan.md` – Implementation roadmap
4. `docs/README.md` – Complete documentation index

**Procedures:** `.agent/sops/`  
**Common errors:** `.claude/CRITICAL_MISTAKES.md`  
**System docs:** `.agent/system/`

---

## Ready to Code?

1. Complete the pre-work checklist.
2. Start the session with the mandatory protocol prompt.
3. Cascade will handle sub-agents, documentation, and context optimization automatically—just enforce confirmations.

🚀 **Happy coding with optimized context!**

---

## UI & Frontend Design (SuperDesign)

For standalone HTML/design prototypes, use the SuperDesign workflow.

- **Guide:** `.claude/skills/projectpulse/superdesign-ui-generator.md`
- **When to use:** “design UI prototype”, “create HTML mockup”, or explicit “use SuperDesign workflow”
- **Output:** Standalone HTML files under `.superdesign/design_iterations/`
- **Note:** For React components inside ProjectPulse, use `.claude/skills/projectpulse/ui-generation-workflow.md`.

